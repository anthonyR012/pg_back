# INSTALL PYTHON

sudo apt update
sudo apt-get install software-properties-common
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt-get update
sudo apt install python3.11
sudo apt install python3.11-venv && python3.11-dev

# GIT CONF
ssh-keygen -t rsa -b 4096 -C "pablocfes@gmail.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa

cat ~/.ssh/id_rsa.pub

Añade la clave SSH a tu cuenta de GitHub o GitLab (u otro servidor Git):

Inicia sesión en tu cuenta en la plataforma de alojamiento del repositorio (GitHub, GitLab, etc.).
Navega a la configuración de tu cuenta y encuentra la sección de "Claves SSH" o "SSH Keys".
Agrega una nueva clave y pega la clave pública que copiaste en el paso anterior.

git clone git@github.com:blackfenixsoft/PonteGlamBackend.git


# CREATE VIRTUAL ENVIRONMENT
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# NGINX
sudo apt install ufw
sudo ufw enable
sudo apt install nginx   
sudo ufw allow 'Nginx FULL'
sudo reboot

# DAPHNE SERVICE
cd /etc/systemd/system
sudo nano ponteglam.service


[Unit]
Description=myproject Daphne Service
After=network.target
[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/pg/PonteGlamBackend
ExecStart=/home/ubuntu/pg/venv/bin/daphne -p 9001 -u /tmp/daphne.sock ponteglam.asgi:application
#edit myproject
#If your daphne is not installed globally and is inside your #environment, enter the complete path to daphne
#ExecStart=/home/ubuntu/myproject/env/bin/daphne -p 9001
#          myproject.asgi:application
[Install]
WantedBy=multi-user.target

sudo systemctl daemon-reload
sudo systemctl start ponteglam
sudo systemctl enable ponteglam
sudo systemctl status ponteglam


# then configure nginx to listen to the port of daphne

upstream channels-backend {
 server localhost:9001;
}

server {
 listen 80;
 server_name ponteglam.jpgonzalez.info;
 location = /favicon.ico { access_log off; log_not_found off; }
 location /static/ {
 autoindex on;
 alias /home/ubuntu/pg/static/;
  }

 location / {
 include proxy_params;
 proxy_pass http://unix:/tmp/daphne.sock;
 }
#path to proxy my WebSocket requests
location /chat/ {

 proxy_pass http://channels-backend;
 proxy_http_version 1.1;
 proxy_set_header Upgrade $http_upgrade;
 proxy_set_header Connection "upgrade";
 proxy_redirect off;
 proxy_set_header Host $host;
 proxy_set_header X-Real-IP $remote_addr;
 proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
 proxy_set_header X-Forwarded-Host $server_name;
 }
}

sudo ln -s /etc/nginx/sites-available/django-daphne.conf /etc/nginx/sites-enabled/
sudo systemctl restart nginx
sudo systemctl restart ponteglam


# INSTALL POSTGRESQL ON SERVER

sudo apt update

sudo apt install postgresql postgresql-contrib

sudo systemctl start postgresql.service

sudo -i -u postgres

psql

CREATE DATABASE dbpg;

ALTER USER postgres WITH PASSWORD 'postgres';



# CELERY AND REDIS

sudo apt-get install redis-server
sudo systemctl restart redis.service


# STATIC FILES

sudo usermod -a -G ubuntu www-data
sudo chown -R ubuntu:www-data /home/pg/static


export XAUTHORITY=$HOME/.Xauthrity